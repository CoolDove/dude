package builtin

@private
color_yellow :: Color{ 1, 1, 0, 1 }

@private
color_white :: Color{ 1, 1, 1, 1 }

@(load="res/shader/builtin_mesh_opaque.shader")
shader_default : Shader

@(load="res/texture/dude.png")
dude_logo :: Texture{
    scale = 1.0,
    tint = color_white,
}

mat_default := Material {
    shader = shader_default,
}

mat_ext := Material {
    shader = extends.shader_default,
}